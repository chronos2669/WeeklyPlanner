// Preload runs in an isolated context before the renderer loads.
// Currently empty: the planner persists via localStorage, which
// Electron's Chromium profile handles automatically and keeps
// across app restarts.
